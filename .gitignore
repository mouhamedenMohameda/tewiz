node_modules
.env
.env.local
*.log
dist
build
.DS_Store
uploads

# Build artifacts (should never be committed)
.next/
.expo/
*.tsbuildinfo
packages/shared-types/src/*.js
# stray root expo config
/app.json

# Local Claude Code settings
.claude/

# Signing keystores — never commit (losing one is unrecoverable, leaking one is worse)
*.jks
*.keystore

# Visuels pub (sources + rendus) : local uniquement
ads/
